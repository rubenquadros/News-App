<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="AppTheme.ToolbarTheme">
        <item name="colorControlNormal">@color/colorWhite</item>
    </style>

    <style name="AppTheme.PopupOverlay"
        parent="ThemeOverlay.AppCompat.Light"/>
    
    <style name="AppTheme.TransparentActionBar"
        parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorPrimary">@android:color/transparent</item>
        <item name="colorPrimaryDark">@android:color/transparent</item>
    </style>

    <style name="BackButtonStyle" parent="Widget.AppCompat.ActionButton">
        <item name="android:minWidth">42dp</item>
        <item name="android:width">42dp</item>
        <item name="android:minHeight">42dp</item>
        <item name="android:height">42dp</item>
    </style>

    <style name="ThemeActionBar"
        parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="colorControlNormal">@color/colorWhite</item>
        <item name="android:actionButtonStyle">@style/BackButtonStyle</item>
        <item name="actionButtonStyle">@style/BackButtonStyle</item>
    </style>

</resources>
